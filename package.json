{
  "name": "playwright-typescript-cucumber-allure-demo",
  "version": "1.0.0",
  "description": "Playwright + TypeScript + Cucumber BDD framework with Allure reporting",
  "scripts": {
    "clean:logs": "rimraf logs",
    "clean:allure": "rimraf allure-results && rimraf allure-report",
    "test": "npm run clean:logs && npm run clean:allure && cucumber-js --config framework/cucumber.js",
    "test:ui": "npm run clean:logs && npm run clean:allure && cucumber-js --config framework/cucumber.js -p ui",
    "test:api": "npm run clean:logs && npm run clean:allure && cucumber-js --config framework/cucumber.js -p api",
    "clean-env": "ts-node ./framework/scripts/clean-env.ts",
    "allure:generate": "npm run clean-env && allure generate allure-results --clean -o allure-report",
    "allure:open": "allure open allure-report",
    "test:report": "npm test && npm run allure:generate && npm run allure:open"
  },
  "devDependencies": {
    "@cucumber/cucumber": "^13.0.0",
    "@cucumber/html-formatter": "^24.0.0",
    "@cucumber/pretty-formatter": "^4.0.0",
    "@faker-js/faker": "^10.5.0",
    "@playwright/test": "^1.61.1",
    "ajv": "^8.20.0",
    "allure-commandline": "^2.43.0",
    "allure-cucumberjs": "^3.10.1",
    "dotenv": "^17.4.2",
    "jsonpath-plus": "^10.4.0",
    "playwright": "^1.60.0",
    "rimraf": "^6.1.3",
    "systeminformation": "^5.31.11",
    "ts-node": "^10.9.2",
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "winston": "^3.19.0"
  }
}
